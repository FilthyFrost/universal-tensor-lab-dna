Original prompt: 现在你继续去做我刚刚提到的需求：需求：不要普通，不要保守，不要小清新参数变体。你要的是疯狂的、非同质化的、每个身体部位都像不同生物突变路线的生成器。

## 2026-05-01 v30 Work Log

- Goal: replace conservative morphology variants with a more extreme, non-homogeneous mutation generator.
- Constraints to preserve: single continuous main manifold, around 3000 faces max, no floating sticker meshes for ears/wings/body identity.
- Planned implementation: amplify head/side/dorsal primitive fields with stronger silhouette ownership and update QA atlas afterward.
- Implemented `applyBodyBlueprint` so chassis DNA now changes actual body silhouette, not just sx/sy/sz scaling.
- Amplified head, side, and dorsal primitive fields for stronger, less homogeneous silhouettes.
- Updated app/docs/package to v30.0.
- QA atlas verification: generated 144 screenshots in `qa-atlas/latest-v30`, browser errors `[]`, face ranges head 2440-2756, side 2440-2532, dorsal 2440-2532, no cases over 3000 faces.
- Visual note: v30 is much more extreme than v29. Mouse-ear AA was adjusted once after inspection because the first pass read too much like a horizontal spike wing.
- Follow-up: shortened GC elephant trunk after user screenshots showed it was too long. Reduced forward Z displacement, reduced downward bend to avoid floor-flattened blade shape, and moved nostril marker meshes closer to the shorter trunk tip.
- Follow-up: reworked CA from long drooping spikes into catfish-inspired barbels. Initial version used anchored tube meshes, then user rejected the assembled look and requested fully integrated modeling. Final version appends six tapered barbel strands directly into the main body `BufferGeometry` and bridges each root to dynamically selected mouth-corner vertices, so extreme bodies keep the barbels on the face. QA: head atlas `qa-atlas/latest-v30-head-integrated-barbels` generated 48 screenshots with browser errors `[]`; CA head case is 2,728 faces. Extreme body check `qa-atlas/catfish-integrated-body-check` rendered Sphere/Slug/Pancake/Toast/Dumpling CA cases with browser errors `[]`; tested face counts stayed at 2,728-2,820.
- Follow-up: rebuilt Dorsal Tensor around marine morphology references instead of generic spike parameters. New contracts include sea anemone bulb crown, branching coral, urchin spine field, hydrothermal vent crater, jellyfish umbrella, seahorse wave fin, nudibranch crown/cerata cluster, shell shield, sailfish spear, lionfish venom rows, and manta-like crest. Key fix: moved dorsal field anchors toward the actual sphere surface so the visible silhouette changes strongly instead of barely affecting vertices. QA: dorsal atlas `qa-atlas/latest-v30-dorsal-marine-final` generated 48 screenshots with browser errors `[]`; tested face range stayed 2,440-2,532.
- Bug fix: user found DNA `CG-TT-CA-AG-GG-GA` stuck on loading. Reproduced with Playwright; root cause was `ReferenceError: colorAttr is not defined` inside `appendCatfishBarbelsToGeometry`, affecting `Head=CA` catfish barbels. Added the missing `geo.attributes.color` handle and wrapped `triggerMutation()` in `try/finally` so future generator exceptions cannot leave the loading overlay permanently visible. Verification: exact DNA renders with loading opacity `0`, poly count `2,820`, and only non-fatal WebGL ReadPixels warnings; head QA atlas `qa-atlas/latest-v30-head-ca-regression` generated 48 screenshots with browser errors `[]`.
- Bug fix: user reported that any `Head=CA` catfish barbel draw could make the body disappear while eyes/shadow remained. Root cause: CA appends new vertices/indices to the shared `BufferGeometry`, but the old sphere `normal` attribute kept the pre-append vertex count. Some WebGL paths can skip the main draw when enabled vertex attributes are shorter than the max indexed vertex. Fix: delete the stale normal attribute before `computeVertexNormals()` so the normal buffer is recreated at the expanded vertex count. Regression screenshots: `qa-atlas/catfish-invisible-current-normal-fix.png`, `qa-atlas/catfish-user-invisible-normal-fix.png`, `qa-atlas/catfish-previous-loading-normal-fix.png`; loading opacity stayed `0`, poly count stayed `2,820`, significant browser logs `[]`.
